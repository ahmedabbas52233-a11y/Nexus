# URL of the deployed backend API (Render/Heroku/etc). Include /api suffix.
VITE_API_URL=http://localhost:5000/api

# Base URL of the deployed backend for Socket.IO + static file (uploads) access
VITE_SOCKET_URL=http://localhost:5000
